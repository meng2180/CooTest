class PCDMetamorph(object):
    def __init__(self, meta_config, operator):
        self.meta_config = meta_config      # range config of every metamorph operator
        self.meta_data_list = []    #
        self.operator = operator    # metamorph operator

